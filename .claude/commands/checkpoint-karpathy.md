---
description: "Run checkpoint-karpathy development for one commit-sized roadmap subphase."
---

Use checkpoint-karpathy development for the requested task.

Instructions:

1. Read project instructions, including `CLAUDE.md` and `AGENTS.md` if present.
2. Select the work mode: greenfield, continuation, feature, bugfix, refactor, or maintenance.
3. Create or update `.checkpoint-karpathy/roadmap.md` for the concrete user goal.
4. Complete exactly one commit-sized roadmap subphase unless a temporary checkpoint is necessary.
5. Update `.checkpoint-karpathy/progress.md`.
6. Run the smallest project-appropriate verification.
7. Run `git status`, `git diff`, stage relevant files only, run `git diff --staged`, and commit.
8. Report the commit and the next subphase.

User request:

$ARGUMENTS
